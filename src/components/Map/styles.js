import {makeStyles} from '@mui/styles';

export const useStyles = makeStyles((theme)=>({
  map:{
    height:'100%',
    width:'100%',
    backgroundColor:'blue',
  },
  container:{
    height:'100%',
    width:'100%',
    position:'relative',
  }
}));
